services:
  simplecdn:
    image: ${DOCKER_REGISTRY-}simplecdn
    build:
      context: .
      dockerfile: SimpleCDN/Dockerfile
    volumes:
    - ../:/data
    environment:
    - CDN_DATA_ROOT=/data